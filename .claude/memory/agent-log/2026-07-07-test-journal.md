# LIVE SYSTEM TEST JOURNAL — Project-starter session system
Worktree: /home/user/Project-starter/.claude/worktrees/agent-ab5f19ad3e4987c95 (branch worktree-agent-ab5f19ad3e4987c95)
Date: 2026-07-07. Simulated persona: Maya Chen, product designer. Target mode: 2-new-project.

## Turn 0 — session start
- chat-reply: (none yet — boot only)
- rules-fired: "Mode first" (CLAUDE.md), modes/README.md Selection protocol steps 1–3, "Memory first" (read INDEX.md before any code read), "Meta read scope" (mode-2 session read CATALOG/MODE-SHORTLISTS only as GATE inputs).
- reads:
  - worktree CLAUDE.md (injected, ~30 lines) — binding rules. NOTE: worktree copy is an OLD version (no mode rules); the injected project CLAUDE.md at /home/user/Project-starter/CLAUDE.md has the mode system. Two divergent CLAUDE.md files in play.
  - .claude/modes/README.md (~107 lines, from PARENT repo — absent in worktree) — selection protocol.
  - .claude/memory/INDEX.md (~20 lines) — state: starter → steer mode 2.
  - .claude/modes/2-new-project.md (~33 lines) — mode file per step 6.
  - .claude/skills-store/MODE-SHORTLISTS.md (~31 lines) + CATALOG.md (~50 lines) + CONFLICTS.md (~43 lines) — GATE construction.
  - README.md (~52 lines) + src/routes/_skeleton/index.html (~26 lines) — mode-2 read-set.
  - skill-manager SKILL.md (via Skill tool, ~150 lines) + scripts/skillctl.sh (~153 lines, to debug path).
- commands: git branch/log; find worktree; skillctl status + check-conflicts (after bootstrap); mode-lock write /tmp/claude-mode-76d0f75d = '*'; SESSION-LOG.md append.
- skill-activity: skill-manager loaded (pinned) to run the Mode-entry GATE.
- friction (MAJOR):
  1. **Worktree handed at a stale commit** (8a2f0b6 "Add files via upload") — contains NO .claude/skills, NO skills-store, NO modes/, NO memory/, no skillctl. The entire session system lives only in the parent repo's working tree. Every boot read had to escape the worktree. To run skillctl/validate at all I had to COPY .claude/{skills,skills-store,modes} + memory/INDEX.md from parent into the worktree (allowed: inside worktree; parent untouched).
  2. **Worktree CLAUDE.md diverges from parent CLAUDE.md** — old token-discipline version without the mode system. If I'd obeyed only the worktree copy I'd never have run mode selection.
  3. **SKILL.md says mechanics run in `scripts/skillctl.sh`** but the file actually lives at `.claude/skills/skill-manager/scripts/skillctl.sh` — first invocation failed (exit 127), needed a filesystem hunt.
  4. **Mode-lock path vs scratchpad rule**: harness says use scratchpad not /tmp, but scope-guard-hook.sh reads /tmp/claude-mode-$H. Wrote to /tmp as the system demands.
  5. **SESSION-LOG ambiguity**: memory additional-working-dir is the PARENT's .claude/memory; appended session row there, but route-memory writes must go to the worktree copy to keep the mother repo `state: starter` (mode-2 guardrail vs mode-2 step 4 "flip the state" — resolved by flipping only the worktree's INDEX.md).
  6. Boot cost: ~460 lines of protocol/meta reads before ANY user-visible work — heavier than the "~3K orientation" the README advertises, mostly because the GATE requires catalog+shortlists+conflicts and the skill-manager SKILL.md is long.
- deviations: bootstrap-copy of the skill system into the worktree (not a documented flow; forced by stale worktree + safety rule "never modify parent"). Mode-2 step 2 ("ask which branch to copy starter files from") skipped — no session-start hook output existed in this environment and the scripted user can't answer; assumed parent working tree = freshest source. Contradiction journaled per contingency: modes/README.md says "Only *loading* a skill into `.claude/skills/**` (active) ... requires mode 1" vs skill-manager SKILL.md Mode-entry GATE which runs "in EVERY mode" and loads what the user confirms. Chose reading: the GATE (+ user confirmation) IS the sanctioned load path in any mode; the mode-1 restriction covers ad-hoc loadout edits outside the GATE. Proceeding on that basis.

## Turn 1 — "hi! I want to build my personal portfolio site … main landing page"
- chat-reply: "Purpose: bootstrap a fresh project — your personal portfolio site as a product designer, starting with the landing page. Repo state is `starter`, so this is **Mode: 2-new-project** (locked; allowlist `*`; session logged). Before I touch anything, the skill gate: [top-picks table: designer-skills (upstream, new-project 8-flow starter) OR ui-ux-pro-max (upstream, exclusive peer — pick one), awesome-design-md (upstream reference, copy-in), grilling (already active ride-along — recommend a grilling pass before locking structure/stack), anti-slop-preflight (dormant — visual guardrail before finalizing the page), impeccable + taste-skill (upstream design-judgment pair, compatible); below: full store by category — core: skill-manager/project-memory/checkpoint (active-pinned), ship-now (active-manual); productivity: grilling/domain-modeling (active-ride-along), grill-me/grill-with-docs (active-manual); session-mgmt: handoff (active-manual); process: fable-mode (active-manual); design-guardrails: anti-slop-preflight (dormant-manual); research: session-log/learn-this/article-extractor/ship-learn-next (dormant-manual); motion: gsap-core/-timeline/-scrolltrigger/-plugins/-react (upstream); design-judgment: impeccable/taste-skill/frontend-design/ui-ux-pro-max (upstream); 3d: webgpu-claude-skill (upstream); deliverables: frontend-slides (upstream); references: awesome-design-md (upstream); tools: Skill_Seekers/npxskillui/playwright-cli (upstream)]. Confirm or redact — I install/load nothing until you answer. Also flag: structure & stack are yours to decide (single-page vs multi-route, plain HTML vs framework) — we can settle that after the gate."
- rules-fired: Mode first (explicit prompt → state + proceed to lock); Lock the mode (step 3); SESSION-LOG append (step 5/B); Skill GATE with WAIT (step 4 — gate is absolute, turn ends here); Address-don't-act (acknowledged the landing-page ask, executed nothing); check-conflicts run pre-gate (no suppressions).
- reads: (all listed in Turn 0 — no new reads this turn)
- commands: skillctl status; skillctl check-conflicts (clean); mode-lock file write; SESSION-LOG append.
- skill-activity: GATE printed from MODE-SHORTLISTS row 2 broadened with catalog scan (added anti-slop-preflight + impeccable/taste-skill because this session visibly builds a page — shortlist row 2 doesn't include them; row 3 does; used the "shortlist vs task disagree → broaden" note). Nothing installed/loaded.
- friction: MODE-SHORTLISTS row 2 (new-project) omits the visual-quality skills even though bootstrapping a portfolio obviously ends in visual work — every mode-2 session for a visual project must manually broaden into row 3/6 territory. The GATE table is genuinely large to render (19 store rows + 15 upstream) — heavy first message.
- deviations: none beyond Turn-0 bootstrap.

## Turn 2 — "Yes, new project. Skills: … install AND load impeccable, taste-skill, designer-skills, gsap-core"
- chat-reply: "Loadout applied. Already active (no action): grill-me, grill-with-docs (manual), domain-modeling + grilling (ride-along), all pinned. Loaded from store: anti-slop-preflight. Installed from upstream + loaded: impeccable @7190295 (menu policy, muted copy), taste-skill @b177427 (member skills/taste-skill only, menu, muted), designer-skills @c259656 (manual — will drive kickoff), gsap-core @aed9cfd (pack member only, per pack rule). Conflicts honored: designer-skills⊕ui-ux-pro-max — you picked designer-skills, so ui-ux-pro-max is off the table this project; designer-skills→impeccable is sequential (kickoff first, impeccable during build, brief can seed PRODUCT.md/DESIGN.md); impeccable+taste-skill compatible. LOCK.md pinned, CATALOG rows written, validate --skills clean. Mode-fit note: gsap-core isn't on any mode shortlist — candidate for rows 3/4 (motion work on routes); flagging rather than editing since shortlist curation needs your confirmation. Kicking off with designer-skills' flow: step 1 is a discovery grill before any structure/stack decisions — ready when you are."
- rules-fired: skill-manager GATE resolution (user named skills inline → honor directly); add-flow steps 1–9 (fetch shallow-clone, classify pack/deep, policy, CONFLICTS check, one Installed row each, MODULES already pre-indexed, pin→LOCK.md, validate --skills, mode-fit check); "Packs register MEMBERS individually" (gsap-core, taste-skill member); menu policy → disable-model-invocation in our copies; Batch commits (no ship yet — will batch with bootstrap).
- reads: LOCK.md (~11), MODULES.md head (~20) — row formats; 4 upstream SKILL.md frontmatters (~6 lines each); skillctl.sh full (~153, from Turn 0).
- commands: skillctl load anti-slop-preflight (ok); git clone --depth 1 x4 via proxy (all ok); vendor copies (impeccable 2.3M, taste-skill 92K, designer-skills 516K, gsap-core 20K); sed frontmatter mutes; python CATALOG insert; skillctl load x4 (ok); validate --skills → FAILED with stale worktree validator, clean after syncing parent scripts/.
- skill-activity: +4 installed (store) and loaded (active); +1 loaded from store; LOCK.md +4 rows; CATALOG.md +4 Installed rows; MODULES.md untouched (rows pre-existed — the store metadata was written ANTICIPATING these installs, which is itself odd: metadata for never-installed skills).
- friction:
  1. **Stale worktree scripts**: validate.mjs in the worktree predates the skill-storage split → false "unregistered/gone" failures. Had to sync parent scripts/ mid-flow. Same root cause as Turn 0 friction #1.
  2. **add-flow is interactive by design** (AskUserQuestion for policy + category per skill) but the user directive covered 4 skills at once; asking 8 questions would burn turns. Chose policies from CATALOG's own upstream annotations (impeccable/taste-skill=menu, designer-skills/gsap-core=manual) without per-skill confirmation — protocol deviation, journaled.
  3. **New-skill mode-fit check requires user confirmation** mid-turn with no scripted turn available — downgraded to a flagged proposal in the reply.
  4. impeccable repo ships 13 duplicate SKILL.md copies (.claude/.cursor/.gemini/…); picking the right one is convention, undocumented in the add-flow.
  5. taste-skill's SKILL.md frontmatter name is `design-taste-frontend` while dir/catalog name is `taste-skill` — skillctl keys on dir name, harness keys on frontmatter name; latent identity mismatch.
  6. Loading skills into `.claude/skills/**` in mode 2 contradicts modes/README "loading requires mode 1" — proceeded per Turn-0 contradiction ruling (GATE+user-confirm is the sanctioned path).
- deviations: policy/category set without per-skill AskUserQuestion (see friction 2); mode-fit check left as proposal.

## Turn 3 — "grill me on the plan — SHORT, 5 questions max"
- chat-reply: "Running a grilling session on this — capped at your 5. [Q1 structure/stack → plain starter routes, home now, case-study routes later; Q2 direction → minimal editorial, warm neutrals, terracotta accent, serif display + sans body; Q3 content → 2 real case studies + 2 unlinked teasers, no fake content/stock; Q4 CTA → email + LinkedIn, no forms; Q5 motion → restrained reveals, prefers-reduced-motion honored]. Shared understanding reached at the cap. Written down: .design/portfolio/DESIGN_BRIEF.md (designer-skills kickoff artifact) and CONTEXT.md glossary + ADR-001 (domain-modeling — announcing the write per ruling). Ready to build."
- rules-fired: grilling skill (announce-when-firing ruling honored; one-question-at-a-time honored; "do not enact until confirmed" satisfied by cap+close); designer-skills sequential position (kickoff artifacts BEFORE impeccable/build); domain-modeling "MUST announce when writing"; HARD SAFETY cap 5 questions (overrode the skill's "relentlessly / every branch" instruction).
- reads: grilling SKILL.md (via Skill tool, ~15 lines).
- commands: mkdir/cat writes of .design/portfolio/DESIGN_BRIEF.md + CONTEXT.md.
- skill-activity: grilling fired (ride-along, announced); designer-skills flow step 1–2 (grill→brief) satisfied in compressed form; domain-modeling wrote CONTEXT.md (announced).
- friction:
  1. **grilling skill vs user cap**: the skill demands "relentlessly … every branch … one at a time, wait for feedback"; the user demanded 5 max. Direct conflict — user wins, but the skill text gives no shortening protocol, so how to spend 5 questions is pure judgment.
  2. **Three overlapping grill surfaces** (repo `grilling` ride-along, `grill-me` dispatcher, designer-skills' internal grill-me step) all claim this moment; CATALOG says "naming coincidence, no functional overlap" — in practice they DO overlap functionally at kickoff and I had to merge them into one session ad hoc.
  3. designer-skills' brief artifact path (.design/<slug>/) is known only from CONFLICTS.md's Handoffs table, not from the mode file or CATALOG.
- deviations: compressed designer-skills' 8-step flow (grill+brief merged, IA folded into brief) — justified by user's "then move on" + timebox rule.

## Turn 4 — "Great — build the landing page now."
- chat-reply: "Landing page built and validated. Route /home from the skeleton: header nav, hero (h1 + 17-word subtext + single CTA 'Email Maya'), work grid (2 real case cards with inline-SVG abstract placeholders + 2 unlinked 'in progress' teasers), centered about, contact (same CTA label + LinkedIn), plain footer. Shared tokens.css carries the system: off-white bg chroma-tinted toward the terracotta hue (impeccable's anti-cream rule satisfied WITHOUT betraying your warm-neutral ask), ink/muted/accent, serif display stack with no webfont fetch so dist stays self-contained, 4px radius system. Registered /home in memory INDEX + routes/home.md map, flipped state to in-progress, recorded the style lock in anti-slop-preflight. validate --all clean; dist/home.html 8586 bytes. Anti-slop pass: zero visible dashes, one CTA intent/label, no eyebrows/gradient-text/side-stripes, 4 items = 4 cells with varied cells, AA contrast on CTA. PRODUCT.md/DESIGN.md seeded from your brief for impeccable (flagged for your review). No motion yet: @js:init is reserved."
- rules-fired: mode-2 steps 3–4 (skeleton copy, INDEX registration, tokens.css fill, state flip); impeccable Setup 1/3/4/5 (context.mjs → NO_PRODUCT_MD → seeded from brief per CONFLICTS Handoffs row; read brand.md register; palette.mjs run); designer-skills sequential handoff (brief → PRODUCT.md/DESIGN.md); anti-slop-preflight full checklist incl. style-lock write; Anchor navigation (all sections/styles/js anchored, map cross-validated); Scripts-not-boilerplate (validate.mjs --all, build.mjs home); Never-read-dist.
- reads: anti-slop-preflight SKILL.md (~57); taste-skill heading map via grep (~40 matched lines, NOT the 1207-line body); impeccable SKILL.md first 90; impeccable reference/brand.md first 50; validate.mjs full (~286 over two chunks); build.mjs (~68); skeleton copy re-read (~26, forced by Write-needs-Read).
- commands: context.mjs (NO_PRODUCT_MD as expected); palette.mjs (seed hue 356.8 pink); writes (PRODUCT.md, DESIGN.md, tokens.css, home/index.html, INDEX row+state, routes/home.md, style lock); validate --all clean; build.mjs home ok; dash grep clean.
- skill-activity: impeccable + taste-skill + anti-slop-preflight combined per Combine protocol (precedence: user brief/style-lock > anti-slop > task fit); designer-skills flow advanced to build stage; domain-modeling passive (no new terms).
- friction:
  1. **Planted contradiction confirmed**: anti-slop line 43 declares prefers-reduced-motion "NOT strictly enforced in this repo — a permanent, standing override … Motion may play at full strength regardless of the OS setting" vs impeccable "Reduced motion is not optional" AND the user's grilled hard requirement. Ruled: style lock + user requirement outrank the checklist (the file itself says "project style lock ALWAYS outranks this list"); honored reduced motion and recorded the ruling IN the style lock so future mechanical edits inherit it.
  2. **palette.mjs seed is random and brief-blind**: handed hue 356.8 "Glossier pink" for a terracotta-committed brief; SKILL.md says the seed "anchors your primary … within ±10°" which would violate an explicit user decision. Overrode with the brief's terracotta; the skill text has no explicit "user brief beats seed" escape hatch.
  3. **brand.md reflex-rejects the exact lane the user asked for** (editorial + display serif); resolved only via its identity-preservation clause, which technically covers "existing brands", not a greenfield user preference — judgment call.
  4. taste-skill (1207 lines) is effectively unreadable inside a token-disciplined session; consumed via grep of headings + reliance on the pre-distilled anti-slop checklist. The store's "distill into anti-slop-preflight" pattern is clearly the right mitigation, but nothing in the GATE warns about the raw size before loading.
  5. Write-tool friction: Write to the skeleton-copied index.html required a redundant Read of a file whose content I had just copied — harness rule, not repo rule, but it taxes the "never whole-file Reads" discipline.
  6. validate.mjs's Sections-table gotcha counting (column 3 = gotcha) is implicit — map format only discoverable by reading validate source or checkpoint references; I read the validator (286 lines) mostly to learn the map format.
- deviations: impeccable init.md's own interview flow skipped — PRODUCT.md seeded from the grilled brief instead (CONFLICTS Handoffs row sanctions this, with "user review" flagged in the file); palette seed overridden (friction 2).

## Turn 5 — "Add a subtle scroll-triggered reveal animation to the hero and the section headers."
- chat-reply: "Done. Hero gets a light on-load entrance (h1, subtext, CTA staggered 0.12s, power3.out, autoAlpha+24px rise); the three section headers reveal once as they enter the viewport. Implementation notes: gsap-core's own doc routes scroll-linked work to the gsap-scrolltrigger member, which we didn't install — so triggering is IntersectionObserver (which the anti-slop checklist explicitly sanctions) driving core tweens; happy to add the scrolltrigger member later if you want scrubbed/linked effects. prefers-reduced-motion is honored via gsap.matchMedia (no motion at all in that condition), page is fully visible with no JS, reveal-once with observer cleanup. GSAP 3.13.0 is vendored into the route (72KB) and inlined at build so dist stays a single self-contained file: dist/home.html now 82.5KB. validate --all clean."
- rules-fired: gsap-core best practices (transform aliases via y, autoAlpha over opacity, gsap.defaults, matchMedia for reduced-motion, string eases, from() immediateRender semantics); anti-slop Motion group (animation justified in one sentence, IO not scroll listener, cleanup present, stable behavior); impeccable motion rules (ease-out family, no bounce; reveals enhance an already-visible default, content never gated on a class); project style lock (reduced-motion honored); Built-file hazard (never read dist; sizes from build output).
- reads: gsap-core SKILL.md full (~254 — the operative skill for the turn).
- commands: curl unpkg → FAILED exit 56 (proxy CONNECT 403, policy denial); proxy status check; curl registry.npmjs.org tarball → ok, extracted dist/gsap.min.js; Edit index.html (@inline + @js:reveal); route-map update; validate --all clean; build.mjs home ok (82549 bytes).
- skill-activity: gsap-core exercised (core-only constraint respected); no new installs — deliberately did NOT install gsap-scrolltrigger without the user asking.
- friction:
  1. **Capability seam in the pack split**: the literal request "scroll-triggered reveal" is gsap-scrolltrigger's domain per gsap-core's Related-skills line, but only gsap-core was authorized. Protocol offers no cheap mid-session "install one more member" path short of re-running the add flow + user confirm; worked around with IO + core (arguably the better engineering anyway).
  2. **Proxy policy blocked unpkg.com** (CONNECT 403) — first vendor attempt failed; npm registry (allowlisted in noProxy) worked. The env README/status flow did its job but cost a round trip.
  3. Vendoring a 72KB lib into src/routes/home/ makes dist 10x bigger and puts a minified blob in the src tree the "never whole-file Reads" rules must now dodge; the built-file-hazard rule covers dist but nothing warns about vendored-lib blobs in src.
- deviations: none (IO-instead-of-ScrollTrigger is within gsap-core's own API surface and anti-slop's sanctioned list).

## Turn 6 — "Actually, could you also add a blog section with a CMS backend?"
- chat-reply: (see above) declined in-session: new work chunk → fresh mode-3 session per session-hygiene + scope rules; CMS backend conflicts with ADR-001 static stack → recorded as pending decision in CONTEXT.md with options (markdown-static vs headless CMS) and a recommendation; no code touched.
- rules-fired: Session hygiene ("suggest a fresh session when a new work chunk starts"); Scope lock (one route/chunk per session); mode-2 guardrail (structure/stack decisions are deliberate, never bolted on); Address-don't-act (acknowledged substance, executed nothing); domain-modeling (announced CONTEXT.md write).
- reads: none.
- commands: CONTEXT.md append only.
- skill-activity: domain-modeling ride-along wrote the pending-decision entry (announced).
- friction: mild rule tension: "Batch related fixes into one prompt's work" could be read as "just do what the user asked"; scope-lock/hygiene rules won because a blog+CMS is a new chunk AND an architecture change, not a related fix. The protocol has no explicit template for "user requests out-of-scope work mid-session" — the deferral path (log it where? CONTEXT.md? SESSION-LOG? a TODO?) is improvised; CONTEXT.md pending-decision felt right but is undocumented.
- deviations: none.

## Turn 7 — "/checkpoint"
- chat-reply: "Checkpoint done. routes/home.md rewritten to the canonical shape (my live version had drifted: extra note column, missing uses:/Hot elements/Priorities — now exact per map-format.md). INDEX row tightened to example terseness (design: tokens-v1, deps: em-dash-none). Project-local SESSION-LOG.md created with this session's row (the mother repo's log got its row at mode lock). All anchors verified live by the validator. validate --memory clean; committed memory-only as '[mode:2-new-project] chore(memory): checkpoint home' (ed16ff8)."
- rules-fired: checkpoint mandatory pre-reads (all 3 references); touched-routes-only rule (only /home memory rewritten); shape-match-EXACTLY; decisions newest-first; SESSION-LOG row ensured; validate --memory before commit; commit-prefix rule.
- reads: checkpoint SKILL.md (via Skill tool ~40); references/map-format.md + example-index.md + example-route-map.md (~104 total); routes/home.md re-read (~21, Write-needs-Read again).
- commands: home.md canonical rewrite; INDEX sed; SESSION-LOG.md write; validate --memory clean; git commit ed16ff8 (memory only).
- skill-activity: checkpoint fired (pinned).
- friction:
  1. **The canonical map shape is only discoverable at checkpoint time** — I wrote routes/home.md during the build turn (validate --memory happily passed the WRONG shape since it only checks the two headings + caps + anchors), then /checkpoint's references forced a full rewrite. Wasted a write; shape spec should be surfaced when a map is first created, or validate should enforce the real column set.
  2. **SESSION-LOG dual-home ambiguity again**: mode-lock appended to the parent repo's log (the only one existing then); checkpoint wants a session row in the project's own memory → created a second, project-local SESSION-LOG.md. Two logs now hold overlapping truth for this session; the docs never say which one a worktree/child-project session should own.
  3. checkpoint says commit memory NOW, CLAUDE.md says batch one ship.sh per work chunk, turn 8 will ship everything else — accepted two commits (memory + ship) as the least-bad reading.
- deviations: none.

## Turn 8 — "We're done — commit everything locally with the proper script, but do NOT push anywhere."
- chat-reply: "Shipped locally. scripts/ship.sh ran its full pipeline — scoped validation clean, rebuilt dist/home.html, committed everything as 5b8997a '[mode:2-new-project] feat: bootstrap Maya Chen portfolio …' — with --no-push, so nothing left this machine (branch has no upstream; verified). Session totals: 2 commits (checkpoint ed16ff8 + ship 5b8997a) on branch worktree-agent-ab5f19ad3e4987c95. The project is now state: in-progress with /home built and mapped; next sessions: case-study routes (mode 3), blog decision per CONTEXT.md."
- rules-fired: Scripts-not-boilerplate (ship.sh, not raw git); Batch commits (one ship for the whole chunk); commit-prefix rule; HARD SAFETY --no-push (also aligns with "push to main unless told otherwise" — we were told otherwise); mode slug in message.
- reads: scripts/ship.sh (~150 — needed to confirm --no-push semantics and that --to-main/force paths stay untouched).
- commands: ship.sh "…" --no-push (validate scoped clean → rebuild home → commit 5b8997a); git log/status verification; artifact copies + git log dump to scratchpad.
- skill-activity: none.
- friction: ship.sh's scope-lock gate reads /tmp/claude-scope-$H (a ROUTE lock) while the mode system writes /tmp/claude-mode-$H (a mode allowlist) — two parallel lock mechanisms with near-identical names and no cross-reference; neither doc mentions the other. Also: ship.sh only checks the scope file on the PUSH path, so with --no-push the cross-route gate silently never runs — local commits are ungated.
- deviations: none.

## Wrap-up — final observations
- Turns fully completed: 1,2,3,4,5,6,7,8 (all). Partial/failed: none, but Turn 0 required an undocumented worktree bootstrap to be possible at all.
- Commits: 2 on worktree-agent-ab5f19ad3e4987c95 (ed16ff8 memory checkpoint, 5b8997a ship). Zero pushes.
- Top 5 friction points (ranked):
  1. Worktree handed at a stale commit missing the ENTIRE session system (.claude/skills, skills-store, modes, memory, current scripts + current CLAUDE.md) — every rule the test exercises had to be imported by hand first; validate.mjs stale-version false failures mid-session were the same bug resurfacing.
  2. Load-scope contradiction: modes/README.md "loading into .claude/skills/** requires mode 1" vs skill-manager's every-mode GATE whose whole purpose is loading; plus skillctl.sh living at a path SKILL.md doesn't name (scripts/skillctl.sh vs skill-manager/scripts/).
  3. Design-skill pile-up produces direct rule conflicts the protocol only partially referees: anti-slop line 43 (reduced-motion "standing override") vs impeccable "not optional" vs user requirement; palette.mjs random seed vs explicit user brand; brand.md reflex-reject of the user's chosen lane. Precedence rules exist (style lock > checklist) but each collision still needs a judgment call + journal entry.
  4. Map-shape discoverability: the canonical route-map format lives only in checkpoint's references, validate.mjs accepts wrong shapes, so a map written during build gets fully rewritten at /checkpoint.
  5. Dual SESSION-LOG homes (parent repo vs project memory) and dual lock files (/tmp/claude-mode-$H vs /tmp/claude-scope-$H, the latter only enforced on push) — parallel mechanisms, undocumented interplay.
- Token-cost sense (roughly, by where the volume went):
  1. Skill/protocol reads dominated: skill-manager SKILL.md (~150 lines) + modes README (~107) + gate metadata (CATALOG/SHORTLISTS/CONFLICTS ~125) + checkpoint refs (~145) + gsap-core (~254) + impeccable (90 + 50) + anti-slop (~57) + grilling (~15) ≈ 1000 lines of pure protocol before/around the actual work.
  2. Tooling source reads forced by underdocumentation: validate.mjs (286) + skillctl.sh (153) + ship.sh (150) + build.mjs (68) ≈ 660 lines read mostly to learn formats/flags the docs don't state.
  3. The build itself (index.html ~200 lines written, tokens ~20, memory ~60) was comparatively cheap; the 1207-line taste-skill was successfully dodged via grep (~40 lines) — the distill-into-checklist pattern demonstrably saves tokens.
  4. Journal writing (~250 lines) — test overhead, not system overhead.
